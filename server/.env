DB_USER=postgres
DB_PASSWORD=3rubendavid
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=meclinic_db

# JWT Configuration
JWT_SECRET=meclinic-super-secret-key-2026-production
JWT_EXPIRY=7d

# ======= EMAIL CONFIGURATION =======
# Modo de operação: 'console' (apenas log) ou 'smtp' (enviar real)
EMAIL_MODE=smtp

# Mailtrap SMTP Configuration
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
EMAIL_USER=b2dd10e75140f9
EMAIL_PASS=d324bd7eeb51ab
SMTP_SECURE=false

# SSL/TLS para HTTPS
SSL_KEY_PATH=./key.pem
SSL_CERT_PATH=./cert.pem

# Node environment
NODE_ENV=development