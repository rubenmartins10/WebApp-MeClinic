/**
 * Default treatment prices (EUR) used for estimate generation.
 * These are fallback values; ideally they should come from the clinic's
 * database configuration (procedimentos table → preco_servico column).
 */
export const PRECO_CARIE = 50;
export const PRECO_EXTRACAO = 40;
export const PRECO_ENDO = 150;
export const PRECO_COROA = 400;
export const PRECO_IMPLANTE = 600;
